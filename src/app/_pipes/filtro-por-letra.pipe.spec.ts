import { FiltroPorLetraPipe } from './filtro-por-letra.pipe';

describe('FiltroPorLetraPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPorLetraPipe();
    expect(pipe).toBeTruthy();
  });
});
